import { chakra, SelectFieldProps } from "@chakra-ui/react"
import React, { ReactNode } from "react"

type SelectProps = {
  children: ReactNode
}
const Select = (props: SelectProps & SelectFieldProps) => {


}

export default Select